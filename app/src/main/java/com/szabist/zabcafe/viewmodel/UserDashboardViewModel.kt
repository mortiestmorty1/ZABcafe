package com.szabist.zabcafe.viewmodel

import androidx.lifecycle.ViewModel
import com.szabist.zabcafe.repository.UserRepository

class UserDashboardViewModel(private val userRepository: UserRepository) : ViewModel() {
    // Example function to fetch user data or any dashboard-specific data
    // Functions will be added here based on the actual requirements
}